# LabMind RAG runtime + dev dependencies.
#
# Install in a Python 3.11+ venv:
#     python3.11 -m venv .venv
#     source .venv/bin/activate
#     pip install -r requirements.txt
#
# To run the service:
#     uvicorn rag.server:app --host 0.0.0.0 --port 8002
#
# To run the tests:
#     pytest tests/
#
# Versions are pinned to the set that the test suite is known to pass on.

# --- runtime ---
chromadb==1.5.9
fastapi==0.136.1
uvicorn[standard]==0.47.0
httpx==0.28.1
numpy==2.4.5
pydantic==2.13.4

# --- dev / test ---
pytest==9.0.3
pytest-asyncio==1.3.0
